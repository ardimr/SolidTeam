apiVersion: v1
kind: Service
metadata:
  name: {{ include "salty-spitoon.fullname" . }}
  labels:
    {{- include "salty-spitoon.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "salty-spitoon.selectorLabels" . | nindent 4 }}
