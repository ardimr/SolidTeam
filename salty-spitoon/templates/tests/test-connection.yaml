apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "salty-spitoon.fullname" . }}-test-connection"
  labels:
    {{- include "salty-spitoon.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "salty-spitoon.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
