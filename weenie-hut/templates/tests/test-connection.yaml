apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "weenie-hut.fullname" . }}-test-connection"
  labels:
    {{- include "weenie-hut.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "weenie-hut.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
